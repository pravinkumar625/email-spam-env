[project]
name = "email-spam-env"
version = "0.1.0"
description = "Email spam classification OpenEnv environment"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv-core>=0.2.0",
    "requests"
]

[project.scripts]
server = "server.app:main"