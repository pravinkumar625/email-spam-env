[project]
name = "email-spam-env"
version = "0.1.0"
description = "Email spam classification OpenEnv environment"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "openai>=1.0.0",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "app:main"